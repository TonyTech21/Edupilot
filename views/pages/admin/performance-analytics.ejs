<%- include('../../partials/header') %>

<div class="analytics-dashboard">
    <div class="dashboard-header">
        <div class="header-content">
            <h1 class="dashboard-title">
                <span class="title-icon">📊</span>
                Performance Analytics
                <span class="title-glow"></span>
            </h1>
            <div class="session-info">
                <% if (activeSession) { %>
                    <div class="session-chip">
                        <i class="fas fa-calendar-alt"></i>
                        <%= activeSession.sessionName %> - <%= activeSession.currentTerm %>
                    </div>
                <% } %>
            </div>
        </div>
        <div class="header-bg"></div>
    </div>
    
    <!-- Analytics Overview Cards -->
    <div class="analytics-overview">
        <div class="overview-card card-primary">
            <div class="card-background"></div>
            <div class="card-content">
                <div class="card-icon">
                    <i class="fas fa-trophy"></i>
                </div>
                <div class="card-data">
                    <h3><%= overallTopStudents.length %></h3>
                    <p>Top Performers</p>
                </div>
            </div>
            <div class="card-glow"></div>
        </div>
        
        <div class="overview-card card-secondary">
            <div class="card-background"></div>
            <div class="card-content">
                <div class="card-icon">
                    <i class="fas fa-school"></i>
                </div>
                <div class="card-data">
                    <h3><%= classPerformance.length %></h3>
                    <p>Active Classes</p>
                </div>
            </div>
            <div class="card-glow"></div>
        </div>
        
        <div class="overview-card card-accent">
            <div class="card-background"></div>
            <div class="card-content">
                <div class="card-icon">
                    <i class="fas fa-book"></i>
                </div>
                <div class="card-data">
                    <h3><%= subjectPerformance.length %></h3>
                    <p>Subjects Analyzed</p>
                </div>
            </div>
            <div class="card-glow"></div>
        </div>
        
        <div class="overview-card card-success">
            <div class="card-background"></div>
            <div class="card-content">
                <div class="card-icon">
                    <i class="fas fa-chart-line"></i>
                </div>
                <div class="card-data">
                    <h3><%= classPerformance.length > 0 ? Math.round(classPerformance.reduce((sum, cls) => sum + cls.averageScore, 0) / classPerformance.length) : 0 %>%</h3>
                    <p>School Average</p>
                </div>
            </div>
            <div class="card-glow"></div>
        </div>
    </div>
    
    <!-- Charts Container with Overflow -->
    <div class="charts-wrapper">
        <div class="charts-container">
            <!-- Term Performance Chart -->
            <div class="chart-card futuristic-card">
                <div class="card-header">
                    <div class="header-left">
                        <h3 class="chart-title">
                            <span class="title-icon">🎯</span>
                            Top 10 Students by Term
                        </h3>
                        <p class="chart-subtitle">Interactive term-based performance analysis</p>
                    </div>
                    <div class="term-selector">
                        <button class="term-btn active" data-term="First Term">
                            <span>1st</span>
                            <div class="btn-glow"></div>
                        </button>
                        <button class="term-btn" data-term="Second Term">
                            <span>2nd</span>
                            <div class="btn-glow"></div>
                        </button>
                        <button class="term-btn" data-term="Third Term">
                            <span>3rd</span>
                            <div class="btn-glow"></div>
                        </button>
                    </div>
                </div>
                <div class="chart-container">
                    <canvas id="termChart"></canvas>
                    <div class="chart-overlay"></div>
                </div>
                <div class="card-footer">
                    <div class="performance-indicator">
                        <span class="indicator-dot"></span>
                        <span>Real-time Performance Tracking</span>
                    </div>
                </div>
            </div>
            
            <!-- Overall Performance Chart -->
            <div class="chart-card futuristic-card">
                <div class="card-header">
                    <div class="header-left">
                        <h3 class="chart-title">
                            <span class="title-icon">🏆</span>
                            Session Top Performers
                        </h3>
                        <p class="chart-subtitle">Overall academic excellence ranking</p>
                    </div>
                    <div class="chart-stats">
                        <div class="stat-item">
                            <span class="stat-value"><%= overallTopStudents.length %></span>
                            <span class="stat-label">Students</span>
                        </div>
                    </div>
                </div>
                <div class="chart-container">
                    <canvas id="overallChart"></canvas>
                    <div class="chart-overlay"></div>
                </div>
                <div class="card-footer">
                    <div class="performance-indicator">
                        <span class="indicator-dot success"></span>
                        <span>Cross-Subject Analysis</span>
                    </div>
                </div>
            </div>
            
            <!-- Class Performance Chart -->
            <div class="chart-card futuristic-card">
                <div class="card-header">
                    <div class="header-left">
                        <h3 class="chart-title">
                            <span class="title-icon">🏫</span>
                            Class Performance Matrix
                        </h3>
                        <p class="chart-subtitle">Comparative class analytics</p>
                    </div>
                    <div class="chart-stats">
                        <div class="stat-item">
                            <span class="stat-value"><%= classPerformance.length %></span>
                            <span class="stat-label">Classes</span>
                        </div>
                    </div>
                </div>
                <div class="chart-container">
                    <canvas id="classChart"></canvas>
                    <div class="chart-overlay"></div>
                </div>
                <div class="card-footer">
                    <div class="performance-indicator">
                        <span class="indicator-dot warning"></span>
                        <span>Trend Analysis Active</span>
                    </div>
                </div>
            </div>
            
            <!-- Subject Performance Chart -->
            <div class="chart-card futuristic-card">
                <div class="card-header">
                    <div class="header-left">
                        <h3 class="chart-title">
                            <span class="title-icon">📚</span>
                            Subject Performance Insights
                        </h3>
                        <p class="chart-subtitle">Multi-dimensional subject analysis</p>
                    </div>
                    <div class="chart-stats">
                        <div class="stat-item">
                            <span class="stat-value"><%= subjectPerformance.length %></span>
                            <span class="stat-label">Subjects</span>
                        </div>
                    </div>
                </div>
                <div class="chart-container">
                    <canvas id="subjectChart"></canvas>
                    <div class="chart-overlay"></div>
                </div>
                <div class="card-footer">
                    <div class="performance-indicator">
                        <span class="indicator-dot info"></span>
                        <span>Dual-Axis Visualization</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Performance Tables with Overflow -->
    <div class="tables-wrapper">
        <div class="tables-container">
            <!-- Top Students Table -->
            <div class="table-card futuristic-card">
                <div class="card-header">
                    <h3 class="table-title">
                        <span class="title-icon">🌟</span>
                        Hall of Excellence
                    </h3>
                    <div class="table-actions">
                        <button class="action-btn">
                            <i class="fas fa-download"></i>
                            Export
                        </button>
                    </div>
                </div>
                <div class="table-container">
                    <table class="futuristic-table">
                        <thead>
                            <tr>
                                <th>
                                    <div class="th-content">
                                        <span>Rank</span>
                                        <i class="fas fa-sort"></i>
                                    </div>
                                </th>
                                <th>
                                    <div class="th-content">
                                        <span>Student</span>
                                        <i class="fas fa-sort"></i>
                                    </div>
                                </th>
                                <th>
                                    <div class="th-content">
                                        <span>Class</span>
                                        <i class="fas fa-sort"></i>
                                    </div>
                                </th>
                                <th>
                                    <div class="th-content">
                                        <span>Average Score</span>
                                        <i class="fas fa-sort"></i>
                                    </div>
                                </th>
                                <th>
                                    <div class="th-content">
                                        <span>Subjects</span>
                                        <i class="fas fa-sort"></i>
                                    </div>
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (overallTopStudents.length > 0) { %>
                                <% overallTopStudents.forEach((student, index) => { %>
                                    <tr class="table-row">
                                        <td>
                                            <div class="rank-badge rank-<%= index + 1 %>">
                                                <span class="rank-number"><%= index + 1 %></span>
                                                <div class="rank-glow"></div>
                                            </div>
                                        </td>
                                        <td>
                                            <div class="student-info">
                                                <span class="student-name"><%= student.studentName %></span>
                                            </div>
                                        </td>
                                        <td>
                                            <span class="class-badge"><%= student.className %></span>
                                        </td>
                                        <td>
                                            <div class="score-display">
                                                <span class="score-value"><%= Math.round(student.averageScore * 100) / 100 %>%</span>
                                                <div class="score-bar">
                                                    <div class="score-fill" style="width: <%= student.averageScore %>%"></div>
                                                </div>
                                            </div>
                                        </td>
                                        <td>
                                            <span class="subject-count"><%= student.totalSubjects %></span>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } else { %>
                                <tr>
                                    <td colspan="5" class="no-data">
                                        <div class="no-data-content">
                                            <i class="fas fa-chart-line"></i>
                                            <span>No performance data available</span>
                                        </div>
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
            
            <!-- Class Performance Table -->
            <div class="table-card futuristic-card">
                <div class="card-header">
                    <h3 class="table-title">
                        <span class="title-icon">🏫</span>
                        Class Performance Matrix
                    </h3>
                    <div class="table-actions">
                        <button class="action-btn">
                            <i class="fas fa-filter"></i>
                            Filter
                        </button>
                    </div>
                </div>
                <div class="table-container">
                    <table class="futuristic-table">
                        <thead>
                            <tr>
                                <th>
                                    <div class="th-content">
                                        <span>Class</span>
                                        <i class="fas fa-sort"></i>
                                    </div>
                                </th>
                                <th>
                                    <div class="th-content">
                                        <span>Average Score</span>
                                        <i class="fas fa-sort"></i>
                                    </div>
                                </th>
                                <th>
                                    <div class="th-content">
                                        <span>Students</span>
                                        <i class="fas fa-sort"></i>
                                    </div>
                                </th>
                                <th>
                                    <div class="th-content">
                                        <span>Performance</span>
                                        <i class="fas fa-sort"></i>
                                    </div>
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (classPerformance.length > 0) { %>
                                <% classPerformance.forEach(cls => { %>
                                    <tr class="table-row">
                                        <td>
                                            <span class="class-name"><%= cls._id %></span>
                                        </td>
                                        <td>
                                            <div class="score-display">
                                                <span class="score-value"><%= Math.round(cls.averageScore * 100) / 100 %>%</span>
                                                <div class="score-bar">
                                                    <div class="score-fill" style="width: <%= cls.averageScore %>%"></div>
                                                </div>
                                            </div>
                                        </td>
                                        <td>
                                            <span class="student-count"><%= cls.studentCount %></span>
                                        </td>
                                        <td>
                                            <% 
                                                let level = 'Poor';
                                                let levelClass = 'poor';
                                                if (cls.averageScore >= 80) { level = 'Excellent'; levelClass = 'excellent'; }
                                                else if (cls.averageScore >= 70) { level = 'Very Good'; levelClass = 'very-good'; }
                                                else if (cls.averageScore >= 60) { level = 'Good'; levelClass = 'good'; }
                                                else if (cls.averageScore >= 50) { level = 'Average'; levelClass = 'average'; }
                                            %>
                                            <span class="performance-badge <%= levelClass %>">
                                                <span class="badge-text"><%= level %></span>
                                                <div class="badge-glow"></div>
                                            </span>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } else { %>
                                <tr>
                                    <td colspan="4" class="no-data">
                                        <div class="no-data-content">
                                            <i class="fas fa-school"></i>
                                            <span>No class performance data available</span>
                                        </div>
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Chart.js CDN -->
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<style>
/* Dashboard Base Styles */
.analytics-dashboard {
    min-height: 100vh;
    background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 50%, #16213e 100%);
    color: #ffffff;
    overflow-x: hidden;
    position: relative;
}

.analytics-dashboard::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: 
        radial-gradient(circle at 20% 80%, rgba(120, 119, 198, 0.3) 0%, transparent 50%),
        radial-gradient(circle at 80% 20%, rgba(255, 119, 198, 0.15) 0%, transparent 50%),
        radial-gradient(circle at 40% 40%, rgba(120, 219, 255, 0.1) 0%, transparent 50%);
    pointer-events: none;
    z-index: 0;
}

/* Dashboard Header */
.dashboard-header {
    position: relative;
    padding: 3rem 2rem;
    background: linear-gradient(135deg, rgba(15, 15, 35, 0.9) 0%, rgba(26, 26, 46, 0.8) 100%);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    overflow: hidden;
}

.header-bg {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, transparent 0%, rgba(120, 119, 198, 0.1) 50%, transparent 100%);
    animation: headerFlow 8s ease-in-out infinite;
}

@keyframes headerFlow {
    0%, 100% { transform: translateX(-100%); }
    50% { transform: translateX(100%); }
}

.header-content {
    position: relative;
    z-index: 2;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 2rem;
}

.dashboard-title {
    font-size: 3rem;
    font-weight: 800;
    background: linear-gradient(135deg, #7877c6 0%, #ff77c6 50%, #77dbff 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    position: relative;
    display: flex;
    align-items: center;
    gap: 1rem;
    margin: 0;
    animation: titleGlow 3s ease-in-out infinite;
}

@keyframes titleGlow {
    0%, 100% { filter: brightness(1) drop-shadow(0 0 10px rgba(120, 119, 198, 0.5)); }
    50% { filter: brightness(1.2) drop-shadow(0 0 20px rgba(120, 119, 198, 0.8)); }
}

.title-icon {
    font-size: 2.5rem;
    animation: iconFloat 2s ease-in-out infinite;
}

@keyframes iconFloat {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
}

.session-chip {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    padding: 0.75rem 1.5rem;
    border-radius: 50px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 600;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
}

/* Overview Cards */
.analytics-overview {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    padding: 2rem;
    position: relative;
    z-index: 1;
}

.overview-card {
    position: relative;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(20px);
    border-radius: 20px;
    padding: 2rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    cursor: pointer;
}

.overview-card:hover {
    transform: translateY(-10px) scale(1.02);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
}

.card-background {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    opacity: 0;
    transition: opacity 0.4s ease;
}

.card-primary .card-background { background: linear-gradient(135deg, rgba(120, 119, 198, 0.2) 0%, rgba(255, 119, 198, 0.1) 100%); }
.card-secondary .card-background { background: linear-gradient(135deg, rgba(255, 119, 198, 0.2) 0%, rgba(120, 219, 255, 0.1) 100%); }
.card-accent .card-background { background: linear-gradient(135deg, rgba(120, 219, 255, 0.2) 0%, rgba(119, 255, 120, 0.1) 100%); }
.card-success .card-background { background: linear-gradient(135deg, rgba(119, 255, 120, 0.2) 0%, rgba(255, 255, 119, 0.1) 100%); }

.overview-card:hover .card-background {
    opacity: 1;
}

.card-content {
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
    gap: 1.5rem;
}

.card-icon {
    width: 70px;
    height: 70px;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.2) 0%, rgba(255, 255, 255, 0.05) 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2rem;
    color: #ffffff;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    transition: all 0.4s ease;
}

.overview-card:hover .card-icon {
    transform: scale(1.1) rotate(10deg);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.4);
}

.card-data h3 {
    font-size: 2.5rem;
    font-weight: 800;
    margin: 0;
    background: linear-gradient(135deg, #ffffff 0%, rgba(255, 255, 255, 0.8) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.card-data p {
    margin: 0;
    color: rgba(255, 255, 255, 0.8);
    font-weight: 500;
}

.card-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
    transform: translate(-50%, -50%);
    opacity: 0;
    transition: opacity 0.4s ease;
}

.overview-card:hover .card-glow {
    opacity: 1;
}

/* Charts Wrapper with Overflow */
.charts-wrapper {
    padding: 2rem;
    position: relative;
    z-index: 1;
}

.charts-container {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    max-height: none;
    overflow: visible;
}

/* Chart Cards */
.chart-card {
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(20px);
    border-radius: 24px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
}

.chart-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 25px 80px rgba(0, 0, 0, 0.4);
    border-color: rgba(255, 255, 255, 0.2);
}

.card-header {
    padding: 2rem;
    background: rgba(255, 255, 255, 0.02);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 1.5rem;
}

.chart-title {
    font-size: 1.5rem;
    font-weight: 700;
    margin: 0;
    color: #ffffff;
    display: flex;
    align-items: center;
    gap: 0.75rem;
}

.chart-subtitle {
    color: rgba(255, 255, 255, 0.6);
    margin: 0.5rem 0 0 0;
    font-size: 0.9rem;
}

.term-selector {
    display: flex;
    gap: 0.5rem;
    background: rgba(255, 255, 255, 0.05);
    padding: 0.5rem;
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.1);
}

.term-btn {
    position: relative;
    padding: 0.75rem 1.5rem;
    border: none;
    border-radius: 8px;
    background: transparent;
    color: rgba(255, 255, 255, 0.7);
    cursor: pointer;
    transition: all 0.3s ease;
    font-weight: 600;
    overflow: hidden;
}

.term-btn.active,
.term-btn:hover {
    color: #ffffff;
    background: rgba(255, 255, 255, 0.1);
    transform: translateY(-2px);
}

.btn-glow {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, rgba(120, 119, 198, 0.3) 0%, rgba(255, 119, 198, 0.2) 100%);
    opacity: 0;
    transition: opacity 0.3s ease;
}

.term-btn.active .btn-glow,
.term-btn:hover .btn-glow {
    opacity: 1;
}

.chart-stats {
    display: flex;
    gap: 1.5rem;
}

.stat-item {
    text-align: center;
}

.stat-value {
    display: block;
    font-size: 1.5rem;
    font-weight: 800;
    color: #ffffff;
}

.stat-label {
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.6);
}

.chart-container {
    padding: 2rem;
    height: 400px;
    position: relative;
}

.chart-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, transparent 0%, rgba(255, 255, 255, 0.02) 50%, transparent 100%);
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.3s ease;
}

.chart-card:hover .chart-overlay {
    opacity: 1;
}

.card-footer {
    padding: 1.5rem 2rem;
    background: rgba(255, 255, 255, 0.02);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.performance-indicator {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.9rem;
}

.indicator-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #7877c6;
    animation: pulse 2s infinite;
}

.indicator-dot.success { background: #77ff77; }
.indicator-dot.warning { background: #ffff77; }
.indicator-dot.info { background: #77dbff; }

@keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.7; transform: scale(1.2); }
}

/* Tables Wrapper with Overflow */
.tables-wrapper {
    padding: 2rem;
    position: relative;
    z-index: 1;
}

.tables-container {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    overflow-x: auto;
    padding-bottom: 1rem;
}

/* Table Cards */
.table-card {
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(20px);
    border-radius: 24px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    overflow: hidden;
    transition: all 0.4s ease;
    min-width: 800px;
}

.table-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 25px 80px rgba(0, 0, 0, 0.4);
}

.table-title {
    font-size: 1.4rem;
    font-weight: 700;
    margin: 0;
    color: #ffffff;
    display: flex;
    align-items: center;
    gap: 0.75rem;
}

.table-actions {
    display: flex;
    gap: 1rem;
}

.action-btn {
    padding: 0.75rem 1.5rem;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 12px;
    color: #ffffff;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.action-btn:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: translateY(-2px);
}

.table-container {
    overflow-x: auto;
    max-height: 600px;
    overflow-y: auto;
}

.futuristic-table {
    width: 100%;
    border-collapse: collapse;
    color: #ffffff;
}

.futuristic-table th {
    background: rgba(255, 255, 255, 0.05);
    padding: 1.5rem 1rem;
    text-align: left;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    position: sticky;
    top: 0;
    z-index: 10;
}

.th-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.9);
}

.futuristic-table td {
    padding: 1.5rem 1rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    transition: all 0.3s ease;
}

.table-row:hover {
    background: rgba(255, 255, 255, 0.03);
    transform: scale(1.01);
}

.rank-badge {
    position: relative;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
    color: #ffffff;
    overflow: hidden;
}

.rank-1 { background: linear-gradient(135deg, #ffd700 0%, #ffed4e 100%); color: #000; }
.rank-2 { background: linear-gradient(135deg, #c0c0c0 0%, #e5e5e5 100%); color: #000; }
.rank-3 { background: linear-gradient(135deg, #cd7f32 0%, #daa520 100%); }
.rank-badge:not(.rank-1):not(.rank-2):not(.rank-3) { 
    background: linear-gradient(135deg, #7877c6 0%, #a855f7 100%); 
}

.rank-glow {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.3) 0%, transparent 70%);
    opacity: 0;
    transition: opacity 0.3s ease;
}

.rank-badge:hover .rank-glow {
    opacity: 1;
}

.student-name {
    font-weight: 600;
    color: #ffffff;
}

.class-badge {
    background: rgba(120, 119, 198, 0.3);
    color: #ffffff;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-weight: 600;
    border: 1px solid rgba(120, 119, 198, 0.5);
}

.score-display {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.score-value {
    font-weight: 800;
    font-size: 1.1rem;
    color: #ffffff;
}

.score-bar {
    width: 100px;
    height: 6px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 3px;
    overflow: hidden;
}

.score-fill {
    height: 100%;
    background: linear-gradient(90deg, #7877c6 0%, #ff77c6 50%, #77dbff 100%);
    border-radius: 3px;
    transition: width 0.8s ease;
}

.performance-badge {
    position: relative;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-weight: 600;
    overflow: hidden;
    display: inline-block;
}

.performance-badge.excellent { background: rgba(119, 255, 120, 0.2); color: #77ff77; border: 1px solid rgba(119, 255, 120, 0.5); }
.performance-badge.very-good { background: rgba(120, 219, 255, 0.2); color: #77dbff; border: 1px solid rgba(120, 219, 255, 0.5); }
.performance-badge.good { background: rgba(255, 255, 119, 0.2); color: #ffff77; border: 1px solid rgba(255, 255, 119, 0.5); }
.performance-badge.average { background: rgba(255, 165, 0, 0.2); color: #ffa500; border: 1px solid rgba(255, 165, 0, 0.5); }
.performance-badge.poor { background: rgba(255, 119, 119, 0.2); color: #ff7777; border: 1px solid rgba(255, 119, 119, 0.5); }

.badge-glow {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, transparent 0%, rgba(255, 255, 255, 0.1) 50%, transparent 100%);
    opacity: 0;
    transition: opacity 0.3s ease;
}

.performance-badge:hover .badge-glow {
    opacity: 1;
}

.no-data {
    text-align: center;
    padding: 3rem;
}

.no-data-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    color: rgba(255, 255, 255, 0.5);
}

.no-data-content i {
    font-size: 3rem;
}

/* Responsive Design */
@media (max-width: 1200px) {
    .charts-container {
        grid-template-columns: 1fr;
    }
    
    .analytics-overview {
        grid-template-columns: repeat(2, 1fr);
    }
}

@media (max-width: 768px) {
    .dashboard-title {
        font-size: 2rem;
    }
    
    .analytics-overview {
        grid-template-columns: 1fr;
        padding: 1rem;
    }
    
    .charts-wrapper,
    .tables-wrapper {
        padding: 1rem;
    }
    
    .card-header {
        flex-direction: column;
        align-items: flex-start;
    }
    
    .term-selector {
        width: 100%;
        justify-content: center;
    }
    
    .chart-container {
        height: 300px;
        padding: 1rem;
    }
    
    .header-content {
        flex-direction: column;
        text-align: center;
    }
}

@media (max-width: 480px) {
    .analytics-overview {
        grid-template-columns: 1fr;
    }
    
    .overview-card {
        padding: 1.5rem;
    }
    
    .card-content {
        flex-direction: column;
        text-align: center;
        gap: 1rem;
    }
    
    .dashboard-title {
        font-size: 1.5rem;
        flex-direction: column;
    }
}

/* Custom Scrollbars */
.tables-container::-webkit-scrollbar,
.table-container::-webkit-scrollbar {
    height: 8px;
    width: 8px;
}

.tables-container::-webkit-scrollbar-track,
.table-container::-webkit-scrollbar-track {
    background: rgba(255, 255, 255, 0.1);
    border-radius: 4px;
}

.tables-container::-webkit-scrollbar-thumb,
.table-container::-webkit-scrollbar-thumb {
    background: linear-gradient(135deg, #7877c6 0%, #ff77c6 100%);
    border-radius: 4px;
}

.tables-container::-webkit-scrollbar-thumb:hover,
.table-container::-webkit-scrollbar-thumb:hover {
    background: linear-gradient(135deg, #9d9cdb 0%, #ff9dd6 100%);
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Chart.js configuration with futuristic theme
    Chart.defaults.font.family = "'Inter', 'Segoe UI', sans-serif";
    Chart.defaults.color = '#ffffff';
    Chart.defaults.backgroundColor = 'rgba(255, 255, 255, 0.1)';
    
    // Futuristic color palette
    const colors = [
        '#7877c6', '#ff77c6', '#77dbff', '#77ff77', '#ffff77',
        '#ff7777', '#c677ff', '#77ffc6', '#ffc677', '#c6ff77'
    ];
    
    const gradientColors = colors.map((color, index) => {
        return {
            start: color,
            end: color + '40'
        };
    });
    
    let termChart, overallChart, classChart, subjectChart;
    
    // Initialize charts with enhanced animations
    initializeTermChart('First Term');
    initializeOverallChart();
    initializeClassChart();
    initializeSubjectChart();
    
    // Term selector functionality
    document.querySelectorAll('.term-btn').forEach(btn => {
        btn.addEventListener('click', function() {
            document.querySelectorAll('.term-btn').forEach(b => b.classList.remove('active'));
            this.classList.add('active');
            
            const term = this.dataset.term;
            updateTermChart(term);
        });
    });
    
    function createGradient(ctx, color1, color2) {
        const gradient = ctx.createLinearGradient(0, 0, 0, 400);
        gradient.addColorStop(0, color1);
        gradient.addColorStop(1, color2);
        return gradient;
    }
    
    function initializeTermChart(term) {
        const ctx = document.getElementById('termChart').getContext('2d');
        const data = getTermData(term);
        
        if (termChart) {
            termChart.destroy();
        }
        
        const backgroundGradients = data.labels.map((_, index) => {
            return createGradient(ctx, colors[index % colors.length], colors[index % colors.length] + '20');
        });
        
        termChart = new Chart(ctx, {
            type: 'bar',
            data: {
                labels: data.labels,
                datasets: [{
                    label: 'Average Score (%)',
                    data: data.scores,
                    backgroundColor: backgroundGradients,
                    borderColor: colors.slice(0, data.labels.length),
                    borderWidth: 2,
                    borderRadius: 12,
                    borderSkipped: false,
                    hoverBackgroundColor: colors.slice(0, data.labels.length),
                    hoverBorderWidth: 3,
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        display: false
                    },
                    tooltip: {
                        backgroundColor: 'rgba(15, 15, 35, 0.9)',
                        titleColor: '#ffffff',
                        bodyColor: '#ffffff',
                        borderColor: '#7877c6',
                        borderWidth: 2,
                        cornerRadius: 12,
                        displayColors: false,
                        callbacks: {
                            label: function(context) {
                                return `Score: ${Math.round(context.parsed.y * 100) / 100}%`;
                            }
                        }
                    }
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        max: 100,
                        grid: {
                            color: 'rgba(255, 255, 255, 0.1)',
                            drawBorder: false
                        },
                        ticks: {
                            color: '#ffffff',
                            callback: function(value) {
                                return value + '%';
                            }
                        }
                    },
                    x: {
                        grid: {
                            display: false
                        },
                        ticks: {
                            color: '#ffffff'
                        }
                    }
                },
                animation: {
                    duration: 1200,
                    easing: 'easeInOutCubic',
                    delay: (context) => context.dataIndex * 100
                },
                interaction: {
                    intersect: false,
                    mode: 'index'
                }
            }
        });
    }
    
    function updateTermChart(term) {
        const data = getTermData(term);
        const ctx = termChart.ctx;
        
        const backgroundGradients = data.labels.map((_, index) => {
            return createGradient(ctx, colors[index % colors.length], colors[index % colors.length] + '20');
        });
        
        termChart.data.labels = data.labels;
        termChart.data.datasets[0].data = data.scores;
        termChart.data.datasets[0].backgroundColor = backgroundGradients;
        termChart.data.datasets[0].borderColor = colors.slice(0, data.labels.length);
        termChart.update('active');
    }
    
    function getTermData(term) {
        const termData = <%- JSON.stringify(termAnalytics) %>;
        const students = termData[term] || [];
        
        return {
            labels: students.map(s => s.studentName),
            scores: students.map(s => Math.round(s.averageScore * 100) / 100)
        };
    }
    
    function initializeOverallChart() {
        const ctx = document.getElementById('overallChart').getContext('2d');
        const students = <%- JSON.stringify(overallTopStudents) %>;
        
        overallChart = new Chart(ctx, {
            type: 'doughnut',
            data: {
                labels: students.map(s => s.studentName),
                datasets: [{
                    data: students.map(s => Math.round(s.averageScore * 100) / 100),
                    backgroundColor: colors.slice(0, students.length),
                    borderColor: '#0f0f23',
                    borderWidth: 4,
                    hoverBorderWidth: 6,
                    hoverOffset: 10
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                cutout: '60%',
                plugins: {
                    legend: {
                        position: 'bottom',
                        labels: {
                            usePointStyle: true,
                            padding: 20,
                            color: '#ffffff',
                            generateLabels: function(chart) {
                                const data = chart.data;
                                return data.labels.map((label, i) => ({
                                    text: `${label} (${data.datasets[0].data[i]}%)`,
                                    fillStyle: data.datasets[0].backgroundColor[i],
                                    strokeStyle: data.datasets[0].backgroundColor[i],
                                    pointStyle: 'circle'
                                }));
                            }
                        }
                    },
                    tooltip: {
                        backgroundColor: 'rgba(15, 15, 35, 0.9)',
                        titleColor: '#ffffff',
                        bodyColor: '#ffffff',
                        borderColor: '#7877c6',
                        borderWidth: 2,
                        cornerRadius: 12,
                        callbacks: {
                            label: function(context) {
                                return `${context.label}: ${context.parsed}%`;
                            }
                        }
                    }
                },
                animation: {
                    animateRotate: true,
                    duration: 1500,
                    easing: 'easeInOutCubic'
                }
            }
        });
    }
    
    function initializeClassChart() {
        const ctx = document.getElementById('classChart').getContext('2d');
        const classes = <%- JSON.stringify(classPerformance) %>;
        
        const gradient = createGradient(ctx, '#7877c6', '#ff77c6');
        
        classChart = new Chart(ctx, {
            type: 'line',
            data: {
                labels: classes.map(c => c._id),
                datasets: [{
                    label: 'Average Score (%)',
                    data: classes.map(c => Math.round(c.averageScore * 100) / 100),
                    borderColor: '#7877c6',
                    backgroundColor: gradient,
                    borderWidth: 4,
                    fill: true,
                    tension: 0.4,
                    pointBackgroundColor: '#ffffff',
                    pointBorderColor: '#7877c6',
                    pointBorderWidth: 3,
                    pointRadius: 8,
                    pointHoverRadius: 12,
                    pointHoverBackgroundColor: '#7877c6',
                    pointHoverBorderColor: '#ffffff'
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        display: false
                    },
                    tooltip: {
                        backgroundColor: 'rgba(15, 15, 35, 0.9)',
                        titleColor: '#ffffff',
                        bodyColor: '#ffffff',
                        borderColor: '#7877c6',
                        borderWidth: 2,
                        cornerRadius: 12,
                        callbacks: {
                            label: function(context) {
                                return `Average: ${context.parsed.y}%`;
                            }
                        }
                    }
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        max: 100,
                        grid: {
                            color: 'rgba(255, 255, 255, 0.1)',
                            drawBorder: false
                        },
                        ticks: {
                            color: '#ffffff',
                            callback: function(value) {
                                return value + '%';
                            }
                        }
                    },
                    x: {
                        grid: {
                            display: false
                        },
                        ticks: {
                            color: '#ffffff'
                        }
                    }
                },
                animation: {
                    duration: 1500,
                    easing: 'easeInOutCubic'
                }
            }
        });
    }
    
    function initializeSubjectChart() {
        const ctx = document.getElementById('subjectChart').getContext('2d');
        const subjects = <%- JSON.stringify(subjectPerformance) %>;
        
        subjectChart = new Chart(ctx, {
            type: 'bar',
            data: {
                labels: subjects.map(s => s._id),
                datasets: [
                    {
                        label: 'Average Score (%)',
                        data: subjects.map(s => Math.round(s.averageScore * 100) / 100),
                        backgroundColor: createGradient(ctx, '#7877c6', '#7877c640'),
                        borderColor: '#7877c6',
                        borderWidth: 2,
                        borderRadius: 8,
                        yAxisID: 'y'
                    },
                    {
                        label: 'Pass Rate (%)',
                        data: subjects.map(s => Math.round(s.passRate * 10000) / 100),
                        backgroundColor: createGradient(ctx, '#ff77c6', '#ff77c640'),
                        borderColor: '#ff77c6',
                        borderWidth: 2,
                        borderRadius: 8,
                        yAxisID: 'y1'
                    }
                ]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        position: 'top',
                        labels: {
                            usePointStyle: true,
                            padding: 20,
                            color: '#ffffff'
                        }
                    },
                    tooltip: {
                        backgroundColor: 'rgba(15, 15, 35, 0.9)',
                        titleColor: '#ffffff',
                        bodyColor: '#ffffff',
                        borderColor: '#7877c6',
                        borderWidth: 2,
                        cornerRadius: 12
                    }
                },
                scales: {
                    y: {
                        type: 'linear',
                        display: true,
                        position: 'left',
                        beginAtZero: true,
                        max: 100,
                        grid: {
                            color: 'rgba(255, 255, 255, 0.1)',
                            drawBorder: false
                        },
                        ticks: {
                            color: '#ffffff',
                            callback: function(value) {
                                return value + '%';
                            }
                        }
                    },
                    y1: {
                        type: 'linear',
                        display: true,
                        position: 'right',
                        beginAtZero: true,
                        max: 100,
                        grid: {
                            drawOnChartArea: false,
                        },
                        ticks: {
                            color: '#ffffff',
                            callback: function(value) {
                                return value + '%';
                            }
                        }
                    },
                    x: {
                        grid: {
                            display: false
                        },
                        ticks: {
                            color: '#ffffff'
                        }
                    }
                },
                animation: {
                    duration: 1200,
                    easing: 'easeInOutCubic',
                    delay: (context) => context.dataIndex * 50
                }
            }
        });
    }
});
</script>

<%- include('../../partials/footer') %>